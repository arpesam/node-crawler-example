## Simple node crawler

This repository will search in imdb the most rated movies of the week. The result will be logged and also persisted in a file calle `imdb-movies.txt` in the root folder.

### Launch
- Clone the repository
- Install dependencies runnning `npm i`
- Run `npm start`
